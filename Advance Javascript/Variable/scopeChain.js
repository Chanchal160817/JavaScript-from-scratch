// Scope Chain :- Used to resolve the value of variable name in javascript .

// Scope chain in JS is lexically define ,which means that we can see what the scope chain will be by looking at the code .

// At the topp of the scope chain is the global scope,which is the window object in the browser .

